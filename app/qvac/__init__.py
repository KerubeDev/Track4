# =============================================================================
# Sentinel-DNS QVAC integration package
# =============================================================================

"""Entrypoints for the local QVAC integration.

The product must use the QVAC runtime for inference. The compatibility server
in :mod:`app.qvac.server` is retained only as a development fixture and must
never be used as evidence of QVAC compliance.
"""
